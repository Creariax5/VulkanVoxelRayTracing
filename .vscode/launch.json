{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "program": "${workspaceFolder}/build/voxEngine.exe",
            //"program": "${workspaceFolder}/build/test/test.exe",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}